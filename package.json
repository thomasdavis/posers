{
  "name": "posers",
  "version": "0.1.0",
  "private": true,
  "description": "A freeform procedural motion engine for VRM humanoids",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "validate": "turbo validate",
    "validate:motions": "turbo validate:motions",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "puppeteer": "^23.11.1",
    "turbo": "^2.3.0",
    "typescript": "^5.7.2"
  },
  "packageManager": "pnpm@10.14.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "bezier-easing": "^2.1.0",
    "simplex-noise": "^4.0.3"
  }
}
